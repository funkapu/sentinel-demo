{
  "name": "notely",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A tiny notes app. Deliberately vulnerable demo for Sentinel — do not deploy.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mixpanel": "^0.18.0",
    "openai": "^4.77.0",
    "stripe": "^17.5.0",
    "zod": "^3.24.1"
  }
}
